__pycache__/
*.py[cod]
.venv/
venv/
.env

# Historical data cache and backtest output are reproducible artefacts.
# These MUST be anchored with a leading slash: a bare `data/` also matches
# jarvis5/data/, which silently kept the exchange loader out of the repo.
/data/
/data_synthetic/
/results/
/reports/
*.tar.gz
*.log

.DS_Store
.idea/
.vscode/
